# TODO: add tpch queries, add perm rewrtten queries, add tpch benchmark script
# TODO: add micro benchmark script for filter, perfect and reg aggs, hj, order by, etc.
# TODO: add lineage querying benchmark
